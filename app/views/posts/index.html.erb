<h2> Here are a list of the questions </h2>
<table>
<% @all_posts.each do |post| %>
<tr>
  <td><%= post.title %></td>
  <td><%= link_to "Show", post_path(post) %></td>
  <td><%= link_to 'Edit', edit_post_path(post) %></td>
</tr>
<% end %>
</table>
